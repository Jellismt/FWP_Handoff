# Public services

REST clients for FWP Public Hub layers and external public services (DNRC, MSL/MSDI, USGS,
NOAA, NRCS / Esri Living Atlas). Anything with `auth: none` lives here.

Layer URLs come from `web/src/config/layers.ts` — components never inline a URL.
